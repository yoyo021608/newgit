```mermaid
graph TD
    subgraph 用户层
        A[用户]
    end

    subgraph 前端层
        B1[登录/注册页面]
        B2[文档管理页面]
        B3[智能问答页面]
    end

    subgraph 路由层
        C1[/api/users]
        C2[/api/documents]
        C3[/api/qa]
        C4[/api/qa/ask-fast]
    end

    subgraph 业务层
        D1[用户认证<br>注册/登录/JWT]
        D2[文档管理<br>上传/列表/删除]
        D3[RAG问答<br>检索/生成/溯源]
        D4[工具调用<br>天气/计算/时间]
        D5[技能调用<br>翻译/总结/代码解释]
    end

    subgraph 检索层
        E1[精确匹配<br>文档名匹配]
        E2[向量检索<br>语义相似度]
        E3[多步检索<br>结果不足时重试]
    end

    subgraph 存储层
        F1[(MySQL<br>用户/文档/历史)]
        F2[(ChromaDB<br>文档向量)]
        F3[(uploads/<br>文档文件)]
    end

    subgraph AI层
        G[通义千问 API]
    end

    A --> B1
    A --> B2
    A --> B3

    B1 --> C1 --> D1 --> F1
    B2 --> C2 --> D2 --> F1
    B2 --> C2 --> D2 --> F3
    B3 --> C3 --> D3 --> E1

    E1 -->|匹配不到| E2
    E2 -->|结果不足| E3
    E2 --> F2
    E3 -->|仍匹配不到| H[返回未找到]

    D3 --> G
    D4 --> G
    D5 --> G
```